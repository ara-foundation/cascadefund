services:
  cascadefund-mongo:
    image: mongo:latest
    container_name: cascadefund-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      # Bind mount host directory to persist MongoDB data
      - /data/cascadefund-mongo:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password
    healthcheck:
      test: mongosh --eval "db.adminCommand('ping')" --quiet
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s

  cascadefund-mongo-express:
    image: mongo-express:latest
    container_name: cascadefund-mongo-express
    restart: unless-stopped
    ports:
      - "8082:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: password
      ME_CONFIG_MONGODB_URL: mongodb://root:password@cascadefund-mongo:27017/
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin
    depends_on:
      cascadefund-mongo:
        condition: service_healthy
